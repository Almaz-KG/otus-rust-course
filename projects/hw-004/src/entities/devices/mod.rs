/// An additional sub module devoted the smart socket device. It contains the
/// socket struct, as well as some helper and utility structs.
pub mod socket;

/// I'm tired to write stub text here, hopefully in  the production code I will not be
/// so boiled with such kind of dummy documentation writing part. Again, as the
/// name of the module states - it contains [thermometer#Thermometer] struct and the helper struct
pub mod thermometer;